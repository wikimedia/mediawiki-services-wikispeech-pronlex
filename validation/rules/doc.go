// Package rules contains a few general validation rule types (rules.go), and project specific validators (validators.go). The validation design and interfaces can be found in package validation.
package rules
